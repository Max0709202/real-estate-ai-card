# Backend API Security

# Deny direct access to config files
<FilesMatch "\.(php|sql|json)$">
    <If "%{REQUEST_URI} =~ m#/config/#">
        Require all denied
    </If>
</FilesMatch>

# CORS headers are handled in PHP for each endpoint to support credentials
# DO NOT set Access-Control-Allow-Origin: * here as it conflicts with credentials

